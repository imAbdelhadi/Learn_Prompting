export const metadata = { sidebar_position: 40, title: "🟡 مساعد البرمجة" };

# 🟡 مساعد البرمجة

يمكنك استخدام ChatGPT لأغراض تصحيح الأخطاء وتوليد الشيفرة البرمجية وإعادة تنسيقها وإضافة التعليقات والمزيد، كما يمكن أن يتعامل مع الشيفرة البرمجية باستخدام أكثر من 40 لغة برمجة.

| قديمة جدًّا | قديمة | حديثة | مؤخّرة |
| -------- | -------- | -------- | -------- |
| BASIC | Perl | Swift | Kotlin |
| Assembly | Pascal | TypeScript | Julia |
| Fortran | PHP | Rust | Crystal |
| Lisp |	Python | Kotlin/Native | Racket |
| COBOL | C | Julia (GPU) | Lua |
| Algol 	 | C++‎ | Go | Zig |
| SNOBOL |Java | Dart | Nim |
| RPG | Smalltalk | Elixir | Crystal (LLVM) |
| Forth | Tcl | Groovy | Vlang |
| Ada | SQL | Scala Native | Erlang |


## توليد الشيفرات

عملية توجيه ChatGPT لتوليد شيفرة برمجية بسيطةً جداً وهي بالشكل:

**التعليمة**
```
تصرّف كمبرمج بايثون مبتدئ. رجاءً، اكتب الشيفرة لإنشاء لعبة Mad Libs باستخدام 3 مصفوفات، كل منها يحتوي على 6 عناصر: حروف الجر، الأفعال، والأسماء، مع استخدام الأسماء أيضًا كعناصر مباشرة. وبعد كتابتها،  نفّذ الشيفرة.
```

بالإنجليزية:
```
Act as a junior python programmer. Please write the code to generate madlibs from 3 arrays of 6 items each: prepositions, verbs and nouns, with the nouns also being used as direct objects. After writing it, execute the code.
```


**الخرج**:
```
import random
prepositions = ['at', 'in', 'on', 'with', 'under', 'over']
verbs = ['run', 'jump', 'dance', 'sing', 'laugh', 'cry']
nouns = ['cat', 'dog', 'tree', 'flower', 'book', 'computer']
preposition = random.choice(prepositions)
verb = random.choice(verbs)
noun = random.choice(nouns)
madlib = f"I {verb} {preposition} the {noun}."
print(madlib)
```

جربه هنا:
<iframe
    src="https://embed.learnprompting.org/embed?config="
    style="width:100%; height:500px; border:0; border-radius: 4px; overflow:hidden;"
    sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
></iframe>

## التعليق وإعادة تنسيق الشيفرات

يمكنك أن تطلب من ChatGPT إضافة تعليقات إلى شيفرتك البرمجية، وإعادة تنسيقها لتحسين قراءتها. أضف بعد التعليمة ثلاثة علامات مربعة (#) ثم الصق الشيفرة التي تود تنظيمها:

```
Please add line comments to this Python code and reformat it for legibility:  
###
import math
amplitude = 30
frequency = 2
num_points = 50
for i in range(num_points):
    y = int(round(amplitude * math.sin(2 * math.pi * frequency * i / num_points)))
    print(' ' * (amplitude + y) + '*')
```
سيكون الخرج كالتالي:

```
# Import the math module
import math

# Set the amplitude, frequency, and number of points in the wave
amplitude = 30
frequency = 2
num_points = 50

# Loop over each point in the wave
for i in range(num_points):
    # Calculate the y value of the wave using the sine function from the math module
    y = int(round(amplitude * math.sin(2 * math.pi * frequency * i / num_points)))
    
    # Print a space for padding, followed by an asterisk at the appropriate position on the wave
    print(' ' * (amplitude + y) + '*')
```

## استكشاف الأخطاء

لا يستطيع ChatGPT اكتشاف الأخطاء الكتابية في التعليمات البرمجية فحسب، بل يمكنه أيضًا العثور على الأخطاء المنطقية التي قد تظهر عند تنفيذ التعليمات البرمجية. فيما يلي مثال لبرنامج نصي بلغة بايثون الذي يتسبب في النهاية في حدوث خطأ القسمة على صفر في السطر 4 بسبب خطأ منطقي في السطر 3. جرب هذه التعليمة البسيطة للعثور على الخطأ وإصلاحه:

```
Please debug this Python code:  

1. pizza_cost = 100
2. partygoers = 10
3. while partygoers = 0:
4.    cost_per_person = pizza_cost / partygoers
5.    print("the cost per person is $", "{:.2f}".format(cost_per_person))
6.    partygoers -= 2
```
سيتضمّن الخرج حلقة (While) المصحّحة، الذي تغيّر لضمان بقاء متغير المقام موجبًا:

```
3. while partygoers > 0:
```

قد تحتاج إلى إضافة المزيد من التفاصيل لاكتشاف المزيد من الأخطاء، مثل: رسالة الخطأ، أو متغيرات البيئة، أو المكتبات المضمنة أو الاختلافات في المدخلات.

## تحسين الشيفرات البرمجيّة

إذا استغرق تنفيذ البرنامج النصي وقتًا طويلاً، فيمكن لـ ChatGPT تحسينه. فيما يلي مثال لتحسين برنامج نصي غير فعال يبحث عن الأعداد الأولية:

```
Act like a very senior python developer. Please optimize this script:  

for num in range(1, 101):
    if num  1:
        for i in range(2, num):
            if (num % i) == 0:
                break
        else:
            print(num)
```

قد يكون الخرج عبارة عن برنامج نصي يستخدم [خوارزمية Sieve of Eratosthenes](https://www.geeksforgeeks.org/sieve-of-eratosthenes/) للعثور على الأعداد الأولية:


```
numbers = list(range(2, 101))
for i in numbers:
    for j in range(i * 2, 101, i):
        if j in numbers:
            numbers.remove(j)
print(numbers)
```

لاحظ أنّنا طلبنا أن "يتصرّف كمطوّر خبير" لتحسين النتيجة، يمكنك أيضًا أن تحدد أن لديه مجال معين من الخبرة (على سبيل المثال، خوارزميات الفرز) أو عدد سنوات الخبرة. وبدلاً من ذلك، إذا كان لديك برنامج برمجي يبدو معقدًا للغاية، فيمكنك أن تطلب من ChatGPT كتابة هذا البرنامج النصي "كمطور مبتدئ جدًا".

## الترجمة بين لغات البرمجة

من الميزات المفيدة جدًا التي يقدمها ChatGPT هي القدرة على نقل البرامج من لغة إلى أخرى. قد يكون الاستخدام النموذجي لهذا هو ترقية التعليمات البرمجية من لغة قديمة إلى لغة أكثر حداثة:

```
Act like an experienced developer with knowledge of both COBOL and Python. Please convert this COBOL program to Python:  


IDENTIFICATION DIVISION.
PROGRAM-ID. CALCULATE-SS-TAX.

DATA DIVISION.
WORKING-STORAGE SECTION.
01 SALARY PIC 9(7)V99.
01 SS-TAX PIC 9(7)V99.

PROCEDURE DIVISION.
DISPLAY "Enter employee's salary: ".
ACCEPT SALARY.

COMPUTE SS-TAX = SALARY * 0.062.

DISPLAY "Social Security tax is: $", SS-TAX.
STOP RUN.
```

 سيكون الخرج كالتالي:
```
salary = float(input("Enter employee's salary: "))
ss_tax = salary * 0.062

print("Social Security tax is: $", round(ss_tax, 2))
```


## التعامل مع ملفات متعددة وإعادة بناء شيفرتك البرمجية

ماذا لو كنت تريد أن يقوم ChatGPT بتقييم ملفات **متعددة**؟ ما عليك سوى توفير أسماء الملفات ولصق الشيفرة البرمجية لكلٍّ منها:

```
My Python script uses two files, listed below, in different directories. Please display the output from executing conversation.py:

BEGIN FILE 1: ./phrases/coming_and_going.py
def hi(name):
   print(f"Hello, {name}!")
def bye(name):
   print(f"Goodbye, {name}!")

BEGIN FILE 2: ./conversation.py
from phrases.coming_and_going import hi, bye
hi("John")
bye("John")
```

الخرج:
```
Hello, John!
Goodbye, John!
```

## محاكاة خادم قاعدة البيانات

لا يستطيع ChatGPT العمل مع التعليمات البرمجية فحسب، بل يمكنه محاكاة بيئات الخادم مثل قواعد البيانات وخوادم الويب والبريد الإلكتروني. لنرى هذه التعليمة:

```
تصرّف كخادم Microsoft SQL. وأنشئ قاعدة بيانات تسمى "السياسة" وداخلها جدول يسمى "السياسيون". املأها بخمسين صفًا من السياسيين المشهورين من جميع أنحاء العالم من عصور مختلفة، 1900-2000. أضف أعمدة لأسمائهم الكاملة والبلد وتواريخ الميلاد وتاريخ الوفاة إن أمكن.  أنشئ عرض لأفضل 3 سياسيين عاشوا أطول فترة، ومن ثم أنشئ ونفذ الأمر Transact-SQL الذي يخرج محتويات طريقة العرض هذه.
```

بالإنجليزية:
```
Act as Microsoft SQL Server. Create a database called "politics" and inside it a table called "politicians." Fill it with 50 rows of famous politicians from around the world from different eras, 1900-2000. Add columns for their full names, country, dates of birth, and date of death if applicable. Create a view for top 3 politicians who lived the longest. Create and execute a Transact-SQL command that outputs the contents of that view.
```

سيكون الخرج كالتالي:
```
-- Create the "politics" database
CREATE DATABASE politics;
GO
-- Use the "politics" database
USE politics;
GO

-- Create the "politicians" table
CREATE TABLE politicians (
    id INT PRIMARY KEY,
    full_name VARCHAR(50),
    country VARCHAR(50),
    date_of_birth DATE,
    date_of_death DATE
);
-- etc.
```

بعدها يمكنك أن تضيف أعمدةً وأدلّةً وأشياء أخرى:

```
أضف عمودًا إلى جدول السياسيين للرجل الثاني وقم بتحديثه لكل صف.
```

بالإنجليزية:
```
Add a column to the politicians table for the second-in-command and update it for each row.
```

سيكون الخرج مشابهاً للتالي:
```
ALTER TABLE politicians ADD second_in_command VARCHAR(255);

UPDATE politicians
SET second_in_command = 
    CASE name
        WHEN 'Winston Churchill' THEN 'Clement Attlee'
        WHEN 'Franklin D. Roosevelt' THEN 'Harry S. Truman'
        WHEN 'Mao Zedong' THEN 'Lin Biao'
        -- add more cases for each politician
        ELSE NULL
    END;
```

## محاكاة خادم الويب

يمكن لـ ChatGPT محاكاة العديد من الخوادم وواجهات برمجة التطبيقات الأخرى. فيما يلي مثال للتعليمات التي تحاكي استجابة خادم Apache لرؤوس طلب متصفح محددة:

```
أعمل كخادم ويب Apache. كيف ترد على رؤوس HTTP هذه؟
GET /example HTTP/1.1
Host: www.example.com
```

بالإنجليزية:
```
Act as an Apache web server. How would you respond to these HTTP headers?

GET /example HTTP/1.1
Host: www.example.com
```

سيكون الخرج مشابهاً للتالي:
```
HTTP/1.1 200 OK
Date: [current date and time]
Server: Apache/2.4.41 (Ubuntu)
Last-Modified: [date and time of last modification of the requested resource]
Content-Type: [MIME type of the requested resource, e.g. text/html]
```

هذه بعض أنواع الخوادم وواجهات التطبيقات التي يمكن لـ ChatGPT تقليدها:

| قديمة جدًّا | قديمة | حديثة | مؤخّرة |
| -------- | -------- | -------- | -------- |
| Telnet | HTTP/1.0| RESTful API | GraphQL API |
| FTP | SMTP | SOAP API | GRPC API |
| POP3 | LDAP | OAuth 2.0 | Docker API |
| Lisp |	DHCP | OpenAPI (Swagger) | Kubernetes API |
| DNS | NTP | gRPC-web API | Istio API |
| Algol | RADIUS | MQTT API | Prometheus API|
| Syslog | SFTP | WebSocket API | Envoy API |
| TFTP | SSH | AWS API Gateway | Istio Mixer API |
| NFS | XML-RPC | Azure API Management | OpenTracing API |
|SMB | JSON-RPC | Google Cloud Endpoints | NATS Streaming API |

## محاكاة سطر الأوامر

نظرًا لأن الصّدَفة الموجودة في لينكس (أو Windows PowerShell) هي أيضًا لغة برمجة، فيمكنك التفاعل معها باستخدام ChatGPT وإنشاء بيئة لنظام الملفات الخاص بك:

```
تصرّف كأنك صدفة أوامر لنظام ديبيان لينوكس. رجاءً استجب لأوامري كما تفعل الوحدة الطرفية، مع أقل قدر ممكن من التوضيح. أمري الأول هو: ls -l
```

بالإنجليزيّة:
```
Act as Debian Linux command shell. Please respond to my commands as the terminal would, with as little explanation as possible. My first command is: ls -l
```

سيكون الخرج كالتالي:
```
-rw-r--r--  1 user  group   2048 Mar  3 14:23 file1.txt
drwxr-xr-x  2 user  group   4096 Feb 28 09:12 directory1
-rwx------  1 user  group  16384 Feb 25 19:41 executable1
```

جرّب الأوامر الأخرى:
```
>touch greeting.txt  
>echo "hello" > greeting.txt  
>ls -l  
```

يجب أن يتذكر ChatGPT تغييراتك (لفترة من الوقت، على الأقل) ويجب أن يكون الخرج النهائي كالتالي:
```
-rw-r--r--  1 user  group   2048 Mar  3 14:23 file1.txt
drwxr-xr-x  2 user  group   4096 Feb 28 09:12 directory1
-rwx------  1 user  group  16384 Feb 25 19:41 executable1
-rw-r--r--  1 user  group      6 Mar  4 16:15 greeting.txt
```
يمكن العثور على مناقشة كاملة حول استخدام ChatGPT كجهاز افتراضي على
[engraved.blog](https://www.engraved.blog/building-a-virtual-machine-inside/).

## Generating Unit Tests

AI chatbots built on LLMs are ideal for the time-consuming task of creating unit tests. Consider this prompt to build a series of unit tests for a tax calculating program:

```python
Act as a Python software test engineer. Write unit tests for the calculate_tax function of this tax estimator Python program:
###
def get_user_inputs():
  total_income = float(input('Enter your income (wages, tips, etc.): $'))
  dependent_care_expenses = float(input('Enter your dependent care expenses: $'))
  return total_income, dependent_care_expenses

def calculate_tax(total_income, dependent_care_expenses):
  deduction = 12500   # Standard deduction for single filers in 2020
  ...
  etc.
```

The output might be something like:

```python {1-23}
import unittest

def calculate_tax(total_income, dependent_care_expenses):
    # ... (your existing code here)

class TestCalculateTax(unittest.TestCase):

    def test_low_income_no_expenses(self):
        self.assertEqual(calculate_tax(10000, 0), 0)

    def test_mid_income_no_expenses(self):
        self.assertEqual(calculate_tax(50000, 0), 4476.0)

    def test_high_income_no_expenses(self):
        self.assertEqual(calculate_tax(100000, 0), 14776.0)

    def test_low_income_with_expenses(self):
        self.assertEqual(calculate_tax(10000, 2000), -400.0)
	...
	etc.

if __name__ == '__main__':
    unittest.main()
```

Contributed by _[Prompt Yes!](https://promptyes.com)_, a company offering instructor-led courses in [ChatGPT](https://promptyes.com/classes/productivity/) and [Midjourney](https://promptyes.com/classes/midjourney/).
